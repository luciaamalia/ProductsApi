spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5432/banco_users
    username: teste
    password: root123
    driver-class-name: org.postgresql.Driver

  # Configurações do Flyway (opcional)
  # flyway:
  #   enabled: true
  #   baseline-on-migrate: true
  #   locations: classpath:db
