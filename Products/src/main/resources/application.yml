spring:
  application:
    name: Products

  datasource:
    url: jdbc:mysql://localhost:3307/product_banco2
    username: amalia
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
